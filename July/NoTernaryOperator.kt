fun main() {
    var a = 10
    var b = 16
    var max = if(b>a)b else a
    println(max)
    //in koltin we can use if else as an expression thus it allows us to use it as an ternary operator whereas in java we cant

}